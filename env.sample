# Copy this file to .env.local and fill values
# Preview protection (Vercel)
BASIC_AUTH_USER=admin
BASIC_AUTH_PASS=change-me

# Optional: Vercel sets this automatically; no need to define locally
# VERCEL_ENV=
